<div
  class="sale-marquee flex items-center whitespace-nowrap max-w-full text-black"
>
  {% for i in (1..3) %}
    {% for block in section.blocks %}
      <span class="marquee--item px-4">
        {% if block.settings.icon %}
          <img src="{{ block.settings.icon | img_url: '32x32' }}" alt="" class="inline-block mr-2" style="height: 1em; width: auto;" />
        {% endif %}
        <span class="text-[12px] capitalize tracking-tight font-semibold">
          {{ block.settings.label }}
        </span>
      </span>
      {% unless forloop.last %}
        <span class="marquee--separator px-4 capitalize tracking-tight font-semibold">·</span>
      {% endunless %}
    {% endfor %}
  {% endfor %}
</div>

{% schema %}
  {
    "name": "Marquee Labels",
    "max_blocks": 12,
    "class": "announcement-bar-section",
    "enabled_on": {
      "groups": ["header"]
    },
    "blocks": [
      {
        "type": "label",
        "name": "Label",
        "settings": [
          {
            "type": "image_picker",
            "id": "icon",
            "label": "Icon"
          }, {
            "type": "text",
            "id": "label",
            "label": "Label",
            "default": "Free Shipping"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Marquee Labels"
      }
    ]
  }
{% endschema %}